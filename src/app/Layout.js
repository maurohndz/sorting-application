import React from 'react';
import Header from './header';
import Bars from './Bars';

const Layout = (props) => {
    return(
        <div>
            <Header />
            <Bars />
        </div>
    );
}

export default Layout;